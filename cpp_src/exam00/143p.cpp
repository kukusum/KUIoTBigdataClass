


/*
#include <iostream>
using namespace std;

class Exp
{
public:
    bool Equals (Exp b)
    int getValue()
    int getBase()
    int getExp()        //이 객체와 객체 b의 값이 같은지 판별하여 리턴
    
    void 

};

Exp::Exp()
{
    getVa
}





int main()
{
    Exp a(3, 2);
    Exp b(9);
    Exp c;

    cout << a.getValue() << ' ' << b.getValue() << ' ' << c.getValue() << endl;
    cout << "a의 베이스 " << a.getBase() << ',' << "지수" << a.getExp() << endl;

    if(a.equals(b))
        cout << "same" << endl;
    else
        cout << "not same" << endl;

    return 0;
}
*/