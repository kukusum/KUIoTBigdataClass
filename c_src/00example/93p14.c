#include <stdio.h>
#include <string.h>

int main(void)
{
    char coffe[100];        // 문자열 변수선언... (동적할당 or 1000개?10000개?)
    int num;

    cin >> coffee >> num;

    if(strcmp(coffee. "에스프레소") == 0)

    scanf("", );

    return 0;
}