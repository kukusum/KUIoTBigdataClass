#include <stdio.h>

int main(void)
{
    int i, j;

    for (i = 2; j < 10; i++)
    {
        // to Do
        for (j = 1; j < 2; j++)
        {
            // to Do
            printf("%d * %d = %d\t", i, j, i*j);
        }
    }


    return 0;
}